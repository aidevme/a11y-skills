{
  "name": "a11y-skills",
  "version": "0.0.1",
  "private": true,
  "description": "Multi-agent accessibility auditing plugin + skillset for React, Fluent UI v9, and Power Platform surfaces",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "reporters/*"
  ],
  "scripts": {
    "build": "tsc -b packages/framework-detector packages/surface-detector packages/rules-engine packages/rules-react packages/rules-fluent-ui packages/rules-vue packages/rules-angular packages/rules-svelte packages/rules-static-html packages/rules-pcf packages/rules-code-apps packages/hooks-precommit packages/runtime-axe reporters/markdown packages/core reporters/sarif reporters/html packages/context-gen",
    "pretest": "npm run build",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "docs:rules": "node packages/core/scripts/gen-rule-reference.mjs",
    "validate:skills": "node scripts/validate-skills.mjs",
    "validate:manifests": "node scripts/check-manifests.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  }
}
